[project]
name = "stowage"
version = "0.1.0"
description = "3D 配载求解微服务 — 整柜装箱 CLP + 拼柜 + LIFO 多客户卸货顺序"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "pydantic>=2.7",
    "py3dbp>=1.1.2",
]

[tool.uv]
package = false
