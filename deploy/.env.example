# 生产环境配置示例。复制到 .env 后填写真实值。
# .env 文件不要 commit 到 git。

POSTGRES_DB=xqt_saas
POSTGRES_USER=xqt
POSTGRES_PASSWORD=change_me_in_production

# 暴露端口（生产建议放到 80 + 8080 反代，外部只暴露 80）
WEB_PORT=80

# 严格开关：生产必须保持 true
RATES_STRICT_QUOTE=true
APP_CARRIER_STRICT_GATEWAY=true
