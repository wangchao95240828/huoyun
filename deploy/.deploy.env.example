# deploy/.deploy.env — 复制到 deploy/.deploy.env 后填上真实值
# .deploy.env 已加 .gitignore，密码不会泄到仓库。

DEPLOY_HOST=root@8.148.227.76
SSH_PORT=22

# 可选：密码部署（不设则用 SSH key）
# 设了之后 deploy-native.sh 会自动用 sshpass，避免反复输入
# DEPLOY_PASSWORD=Seeker@admin_0716

# 兼容旧 docker 路线 deploy.sh 用：
DEPLOY_PATH=/opt/xqt-saas
